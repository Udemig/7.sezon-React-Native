/**
 * Type Annotation:
 * Javascriptte değişkenin türünü belirlemek için bir özellik yoktur.
 * Typscriptte'de tanımllana değişkenlerin tipleinin belirleyebiliyoruz.
 *
 *
 *
 */

let surname: string = "yıldız";

surname = "demir";

// surname=[]

let age: number = 60;
age = 30;

// age="50"

/**
 * JS veri tipleri
 * string
 * number
 * boolean
 * undefined
 * null
 * object
 * * array
 * * function
 * bigint(kullanılmaz çok)
 * symbol(kullanılmaz çok)
 *
 */

let name2: string = "30";

let age2: number = 40;

let has_licence: boolean = true;

let student: object = {
  id: "1",
  name: "ahmet",
  is_graduated: false,
};

let value: null = null;

let anathor_value: undefined;
